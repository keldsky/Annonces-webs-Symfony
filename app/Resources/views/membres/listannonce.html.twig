{% extends '::layout_login.html.twig' %}

{% block menu  %}
    <div class="row profile">
        <div class="col-md-3">
            <div class="profile-sidebar">



                <div class="profile-usermenu">
                    <ul class="nav">
                        <li class="active">
                            <a  href="{{ path('dashboard') }}">
                                <i class="glyphicon glyphicon-home"></i>
                                Mes annonces </a>
                        </li>
                        <li>
                            <a href="{{ path('user') }}">
                                <i class="glyphicon glyphicon-user"></i>
                                Mes infos </a>
                        </li>
                        <li>
                            <a href="#" target="_blank">
                                <i class="glyphicon glyphicon-envelope"></i>
                                Mes messages </a>
                        </li>
                        <li>
                            <a href="#">
                                <i class="glyphicon glyphicon-flag"></i>
                                Aide </a>
                        </li>
                    </ul>
                </div>

            </div>
        </div>
        <div class="col-md-9">
            <br class="profile-content">
                <a href="{{ path('deposer_annonce') }}">Déposer une annonce </a></br></br>
                {% for client in annonc %}

                    {{ client.titre }}  {{ client.texte }} {{ client.prix }} {{ client.codepostal }} {{ client.adresse }}</br>

                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
